const emailInput=document.querySelector('#email');const checkButton=document.querySelector('#check');const results=document.querySelector('#results');const resetButton=document.querySelector('#reset');const toast=document.querySelector('#toast');const formatStatus=document.querySelector('#format-status');const domainStatus=document.querySelector('#domain-status');const disposableStatus=document.querySelector('#disposable-status');const resultEmail=document.querySelector('#result-email');const localPart=document.querySelector('#local-part');const resultDomain=document.querySelector('#result-domain');

const disposableDomains=new Set(['mailinator.com','guerrillamail.com','10minutemail.com','temp-mail.org','tempmail.com','yopmail.com','sharklasers.com','getnada.com','maildrop.cc']);
function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)}
function validEmail(value){return /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/i.test(value)}
function runCheck(){const value=emailInput.value.trim().toLowerCase();if(!validEmail(value)){showToast('Enter a valid email address');emailInput.focus();return}const parts=value.split('@');const domain=parts[1];formatStatus.textContent='VALID';domainStatus.textContent='VALID DOMAIN';disposableStatus.textContent=disposableDomains.has(domain)?'LIKELY DISPOSABLE':'NOT ON LIST';resultEmail.textContent=value;localPart.textContent=parts[0];resultDomain.textContent=domain;results.classList.remove('hidden');results.scrollIntoView({behavior:'smooth',block:'start'});showToast('Email check completed')}
checkButton.addEventListener('click',runCheck);emailInput.addEventListener('keydown',e=>{if(e.key==='Enter')runCheck()});resetButton.addEventListener('click',()=>{results.classList.add('hidden');emailInput.value='';emailInput.focus();window.scrollTo({top:0,behavior:'smooth'})});
